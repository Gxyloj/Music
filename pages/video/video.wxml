<!--pages/video/video.wxml-->
<wxs src='../../utils/format.wxs' module="format"></wxs>
<view class="video">
  <view class="item" wx:for="{{topMVs}}" wx:key="id">
    <video-item-v1 item="{{item}}" 
                   bindtap="handleVideoClick" 
                   data-item="{{item}}">
    </video-item-v1>
  </view>
</view>